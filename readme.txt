This code demonstract tcp communication between client and server. Server is capable to communicate with multiple clients at once. Supported platform is Ubuntu. To build server and client binary use the following commands on ubuntu: 
gcc server.c -o server
gcc client.c -o client
